# Copy this file to .env.local for local development.
# Set these as environment variables in the Vercel dashboard
# under Settings > Environment Variables.
#
# NEVER commit .env.local or any file containing the actual hash.

# bcrypt hash of the team password (cost factor 12).
# Generate with:
#   node -e "require('bcryptjs').hash('YOUR_PASSWORD', 12, (e,h) => console.log(h))"
TEAM_PASSWORD_HASH=
